# Build output
bin/
obj/

# Packaged bundles. The arm64 and x64 builds together are over 200 MB and the
# archive nearly 90 — regenerated by Packaging/build-mac.sh whenever needed,
# and belonging on a release rather than in the history.
dist/

# The generated icon. Written by Packaging/make-icon.ps1 from code that is
# tracked, so the binary itself does not need to be.
Packaging/JinxyMac.icns

# Editors and OS
.vs/
.idea/
*.user
.DS_Store

# Subagent-driven-development scratch: ledger, briefs, review packages.
.superpowers/
.tools/
